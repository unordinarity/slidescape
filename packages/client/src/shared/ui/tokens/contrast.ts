import { createTheme } from '@stitches/react'

const low = createTheme({
  borderWidths: {
    single: '1px'
  }
})

const high = createTheme({
  borderWidths: {
    single: '1px'
  }
})

export const contrast = {
  low,
  high,
}
